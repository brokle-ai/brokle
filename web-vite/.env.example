# Build-time constants only. Per-env values go in runtime /config.js
# (see public/config.js.template + docker-entrypoint.d/). Never add
# API_URL, SENTRY_DSN, or other per-env settings here.

# Dev-server proxy target for /api/v1 and /v1 paths.
VITE_PROXY_TARGET=http://localhost:8080
