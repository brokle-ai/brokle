// Placeholder shape for the dashboard OpenAPI surface. Regenerate
// from a running backend via `pnpm gen:api`. This stub lets `client.ts`
// compile before the first generation run; once real types land,
// `createClient<paths>()` gains path-level inference across the whole
// surface.

export interface paths {}
export interface components {}
export type operations = Record<string, never>
