/**
 * Re-export TimeRangePicker from shared component for backward compatibility.
 * Dashboard-specific usage should import from '@/components/shared/time-range-picker' directly.
 */
export { TimeRangePicker, getTimeRangeDates } from '@/components/shared/time-range-picker'
