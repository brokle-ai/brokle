/**
 * FilterRow for traces
 *
 * Re-exports the shared FilterRow component for backwards compatibility.
 * The traces FilterBuilder now uses the shared component internally.
 */

export { FilterRow } from '@/components/shared/filter-builder'
